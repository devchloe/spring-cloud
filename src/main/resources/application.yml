server:
  port: 8082

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka
    register-with-eureka: false
    fetch-registry: true

hystrix:
  command:
    circuitBreaker:
      requestVolumeThreshold: 2

management:
  security:
    enabled: false